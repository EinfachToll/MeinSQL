I never found an easy to use and free frontend for MySQL, so I made one for myself.

To compile, you need the dev packages of the Qt framework.
Then, type
    qmake && make

In the query field,
F9              submits the query
Page up/down    walks up/down the query history
